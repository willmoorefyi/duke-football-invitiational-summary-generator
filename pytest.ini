[pytest]
# Filter out deprecation warnings from third-party libraries we can't control
filterwarnings =
    # botocore uses datetime.utcnow() which is deprecated in Python 3.12+
    ignore:datetime.datetime.utcnow\(\) is deprecated:DeprecationWarning:botocore
